I'm bad at remembering things, so I'll add stuff here

I want to turn this project into a flat callable api/library, it should only manage rendering.

API stuff:
todo: convert base cmake output to add_library() instead of an executable
todo: public headers should be self-contained, not relying on pch; because it will be compiled against their code
todo: add programmatic methods for mesh and point cloud injection
    -> the path for creating meshes and point clouds should not be from assimp only
        -> createMesh(vertices, indices)
        -> createPCD(points)
    -> note that the registry should be initialized internally
    -> we need to expose the suitable datastructures (Vertex)
todo: make shader paths not build-baked, it should be runtime-resolved or caller-supplied

generic:
todo: implement scene graph tree builder and navigation in gui
todo: implement a parent-child system in the scenegraph; all transforms should still be unflattened and in world-space
todo: implement gui per-mesh transform controls

todo: implement normals loading and lighting model for point clouds
todo: implement changeable shader per-viewport

todo: implement proper multi-lights, look into tiled forward+ rendering
todo: add option for PBR pipeline for meshes

todo: add a .json based user configuration/settings system
    I believe we can just use nlohmann/json